<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Categories</title>
    <meta name="description" content="A clean, responsive categories page built with only HTML, CSS, and vanilla JS." />
    <link rel="icon" type="image/x-icon" href="../../assets/logo/logo-w-a.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="../../stylesheets/catalog.css">
</head>

<body>

    <%- include('../components/navbar') %>
        <!-- Page Head -->
        <section class="pagehead">
            <div class="container">
                <h1 class="page-title">Browse Categories</h1>
                <p class="page-subtitle">Explore topics across development, design, business, and more. Use the search
                    and filters to quickly find what you’re into.</p>

                <!-- Controls -->
                <div class="controls">
                    <div class="search">
                        <span class="icon" aria-hidden="true">
                            <svg class="i20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                                <path d="M21 21l-4.3-4.3" stroke="currentColor" stroke-width="2"
                                    stroke-linecap="round" />
                                <circle cx="11" cy="11" r="7" stroke="currentColor" stroke-width="2" />
                            </svg>
                        </span>
                        <input id="searchInput" type="search" placeholder="Search categories..."
                            aria-label="Search categories" />
                    </div>
                    <div class="sort" role="group" aria-label="Sort categories">
                        <svg class="i20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"
                            aria-hidden="true">
                            <path d="M3 6h18M6 12h12M9 18h6" stroke="currentColor" stroke-width="2"
                                stroke-linecap="round" />
                        </svg>
                        <label for="sortSelect" class="visually-hidden">Sort</label>
                        <select id="sortSelect">
                            <option class="option" value="popular">Most Popular</option>
                            <option class="option" value="name">Name (A→Z)</option>
                            <option class="option" value="rating">Rating Count</option>
                        </select>
                    </div>
                </div>

                <!-- Chips -->
                <div class="chips" id="chips"></div>
            </div>
        </section>

        <!-- Grid -->
        <main class="container">
            <div class="grid" id="grid"></div>
            <div class="pagination" id="pagination" aria-label="Pagination"></div>
        </main>
        <%- include('../components/footer') %>

            <script>
                // ======= Demo Dataset =======
                const CATEGORIES = [

                    // 🔹 CSE
                    { id: 1, name: 'Web Development', url: '/full-stack-web-development', color: '#6ea8fe', desc: 'Learn HTML, CSS, JavaScript, React, and backend frameworks to build dynamic and responsive web applications.', rating: 4.7, tag: 'CSE' },
                    { id: 2, name: 'Machine Learning', url: '/machine-learning', color: '#3ddc97', desc: 'Master supervised and unsupervised learning using Python and data-driven techniques.', rating: 4.6, tag: 'CSE' },
                    { id: 3, name: 'Artificial Intelligence', url: '/artificial-intelligence', color: '#ffd166', desc: 'Understand neural networks, deep learning, NLP, and intelligent automation.', rating: 4.3, tag: 'CSE' },
                    { id: 4, name: 'Data Science', url: '/data-science', color: '#ff6b6b', desc: 'Learn SQL, data visualization, machine learning, and predictive analytics.', rating: 4.5, tag: 'CSE' },
                    { id: 5, name: 'Cybersecurity', url: '/cyber-security', color: '#9b86ff', desc: 'Learn ethical hacking, penetration testing, and digital forensics.', rating: 4.2, tag: 'CSE' },
                    { id: 6, name: 'Azure Cloud Computing', url: '/azure-cloud', color: '#70e1ff', desc: 'Gain skills in Microsoft Azure, cloud deployment, and DevOps.', rating: 4.1, tag: 'CSE' },
                    { id: 7, name: 'AR & VR', url: '/augmented-virtual-reality', color: '#7bdcb5', desc: 'Create immersive 3D experiences using Unity and AR/VR tools.', rating: 3.8, tag: 'CSE' },
                    { id: 8, name: 'Android App Development', url: '/android-app', color: '#f78da7', desc: 'Develop apps using Kotlin, Jetpack, and modern Android tools.', rating: 3.9, tag: 'CSE' },
                    { id: 9, name: 'Blockchain', url: '/blockchain', color: '#6ea8fe', desc: 'Understand blockchain fundamentals, smart contracts, and Web3.', rating: 4.4, tag: 'CSE' },
                    { id: 10, name: 'Graphic Design', url: '/graphic-design', color: '#3ddc97', desc: 'Learn Photoshop, Illustrator, UI design, and branding essentials.', rating: 4.5, tag: 'CSE' },
                    { id: 11, name: 'Java Full Stack', url: '/java-full-stack', color: '#ffd166', desc: 'Master Java, Spring Boot, Microservices, and frontend technologies.', rating: 4.6, tag: 'CSE' },
                    { id: 12, name: 'Python FSD', url: '/python-full-stack', color: '#ff6b6b', desc: 'Learn Python, Django, REST APIs, and full-stack development.', rating: 4.5, tag: 'CSE' },
                    { id: 13, name: 'Salesforce', url: '/sales-force', color: '#9b86ff', desc: 'Master Salesforce CRM, administration, and automation tools.', rating: 4.2, tag: 'CSE' },
                    { id: 14, name: 'SAP', url: '/sap', color: '#70e1ff', desc: 'Learn SAP modules like MM, SD, FICO, and enterprise processes.', rating: 4.0, tag: 'CSE' },
                    { id: 15, name: 'Selenium with Java', url: '/selenium-testing-with-java', color: '#7bdcb5', desc: 'Learn automation testing using Selenium WebDriver and Java.', rating: 4.1, tag: 'CSE' },
                    { id: 16, name: 'ServiceNow (Admin)', url: '/system-admin', color: '#f78da7', desc: 'Learn ServiceNow workflows, ITSM, and system administration.', rating: 4.2, tag: 'CSE' },
                    { id: 17, name: 'UI/UX', url: '/ui-ux', color: '#6ea8fe', desc: 'Learn user-centered design, prototyping, and Figma workflows.', rating: 4.6, tag: 'CSE' },
                    { id: 18, name: 'Web3', url: '/web3', color: '#3ddc97', desc: 'Master decentralized apps, smart contracts, and blockchain tech.', rating: 4.4, tag: 'CSE' },

                    // 🔹 ECE
                    { id: 19, name: 'Embedded Systems', url: '/embedded-system', color: '#a0c4ff', desc: 'Learn microcontrollers, IoT hardware, and embedded C programming.', rating: 3.5, tag: 'ECE' },
                    { id: 20, name: 'Hybrid Electric Vehicle', url: '/hybrid-electric-vehicles', color: '#caffbf', desc: 'Learn EV systems, battery tech, and hybrid vehicle engineering.', rating: 4.8, tag: 'ECE' },
                    { id: 21, name: 'IoT', url: '/iot', color: '#ffd6a5', desc: 'Build IoT projects using sensors, cloud platforms, and automation.', rating: 4.2, tag: 'ECE' },
                    { id: 22, name: 'Robotics', url: '/robotics', color: '#bdb2ff', desc: 'Learn robotics, Arduino, ROS, and automation systems.', rating: 3.9, tag: 'ECE' },

                    // 🔹 Mechanical
                    { id: 23, name: 'AutoCAD', url: '/autocad', color: '#ffc6ff', desc: 'Design engineering drawings and models using AutoCAD.', rating: 3.8, tag: 'Mechanical' },
                    { id: 24, name: 'IC Engine', url: '/ic-engine', color: '#fdffb6', desc: 'Study internal combustion engines and thermodynamics.', rating: 3.7, tag: 'Mechanical' },
                    { id: 25, name: 'Car Design', url: '/car-design', color: '#bde0fe', desc: 'Learn automobile design, aerodynamics, and 3D modeling.', rating: 3.5, tag: 'Mechanical' },
                    { id: 26, name: 'CATIA', url: '/catia', color: '#bde0fe', desc: 'Master CATIA for product design, surfaces, and mechanical modeling.', rating: 4.0, tag: 'Mechanical' },
                    { id: 27, name: 'Supply Chain Management', url: '/supply-chain-management', color: '#bde0fe', desc: 'Study logistics, procurement, operations, and supply chain strategy.', rating: 4.3, tag: 'Mechanical' },

                    // 🔹 Management
                    { id: 28, name: 'Digital Marketing', url: '/digital-marketing', color: '#ffa6c1', desc: 'Learn SEO, Google Ads, SMM, and content marketing.', rating: 4.5, tag: 'Management' },
                    { id: 29, name: 'Human Resource', url: '/human-resource', color: '#6ea8fe', desc: 'Learn recruitment, talent management, and HR analytics.', rating: 4.7, tag: 'Management' },
                    { id: 30, name: 'Business Analytics', url: '/business-analytics', color: '#3ddc97', desc: 'Use Excel, SQL, Power BI, and analytics tools for decisions.', rating: 4.6, tag: 'Management' },
                    { id: 31, name: 'Stock Market', url: '/stock-market', color: '#ffd166', desc: 'Learn trading, technical analysis, and portfolio management.', rating: 4.3, tag: 'Management' },
                    { id: 32, name: 'Marketing & Management', url: '/marketing-management', color: '#ff6b6b', desc: 'Study branding, business strategy, and leadership skills.', rating: 4.5, tag: 'Management' },
                    { id: 33, name: 'Finance', url: '/finance', color: '#9b86ff', desc: 'Learn financial modeling, investment, and budgeting.', rating: 4.2, tag: 'Management' },

                    // 🔹 Biotechnology
                    { id: 34, name: 'Nano Technology', url: '/nano-technology', color: '#70e1ff', desc: 'Study nanomaterials and nano-fabrication methods.', rating: 4.1, tag: 'Biotechnology' },
                    { id: 35, name: 'Genetic Engineering', url: '/genetic-engineering', color: '#7bdcb5', desc: 'Learn CRISPR, DNA tech, and genetic modification.', rating: 3.8, tag: 'Biotechnology' },
                    { id: 36, name: 'Bioinformatics', url: '/bio-informatics', color: '#7bdcb5', desc: 'Learn computational biology, genomics, and biological data analysis.', rating: 4.3, tag: 'Biotechnology' },
                    { id: 37, name: 'Microbiology', url: '/micro-biology', color: '#7bdcb5', desc: 'Study microorganisms and lab techniques.', rating: 3.9, tag: 'Biotechnology' },
                    { id: 38, name: 'Molecular Biology', url: '/molecular-biology', color: '#7bdcb5', desc: 'Learn DNA replication, transcription, and gene regulation.', rating: 4.0, tag: 'Biotechnology' },
                    { id: 39, name: 'Nutrition & Health Management', url: '/nutrition-health-managment', color: '#7bdcb5', desc: 'Learn nutrition science, wellness, and food safety.', rating: 4.2, tag: 'Biotechnology' },
                    { id: 40, name: 'Sensory Science', url: '/sensory-science', color: '#7bdcb5', desc: 'Study food sensory evaluation and analysis.', rating: 4.1, tag: 'Biotechnology' },

                    // 🔹 Pharmacy
                    { id: 41, name: 'Medical Coding', url: '/medical-coding', color: '#f78da7', desc: 'Learn ICD, CPT coding, and healthcare documentation.', rating: 3.9, tag: 'Pharmacy' },
                    { id: 42, name: 'Pharmacovigilance', url: '/pharmacovigilance', color: '#a0c4ff', desc: 'Learn drug safety, regulatory systems, and reporting.', rating: 3.5, tag: 'Pharmacy' },
                    { id: 43, name: 'Clinical SAS', url: '/clinical-sas', color: '#caffbf', desc: 'Learn SAS for clinical trial data analysis.', rating: 4.8, tag: 'Pharmacy' },

                    // 🔹 Commerce
                    { id: 44, name: 'ACCA F4', url: '/acca-f4', color: '#ffd6a5', desc: 'Learn corporate law, business law, and ACCA fundamentals.', rating: 4.6, tag: 'Commerce' },

                    // 🔹 Food & Agriculture
                    { id: 45, name: 'Food Tech', url: '/food-tech', color: '#fdffb6', desc: 'Study food processing, safety, and product development.', rating: 4.4, tag: 'Food-Tech' },
                ];



                const TAGS = ['All', 'CSE', 'ECE', 'Mechanical', 'Management', 'Biotechnology', 'Pharmacy', 'Commerce', 'Food-Tech'];

                // ======= State & Elements =======
                const grid = document.getElementById('grid');
                const searchInput = document.getElementById('searchInput');
                const sortSelect = document.getElementById('sortSelect');
                const chips = document.getElementById('chips');
                const pagination = document.getElementById('pagination');

                const state = {
                    q: '',
                    tag: 'All',
                    sort: 'popular',
                    page: 1,
                    perPage: 8,
                };

                // ======= Utilities =======
                const by = (k) => (a, b) => (a[k] > b[k]) ? 1 : (a[k] < b[k] ? -1 : 0);

                function filterData() {
                    let data = [...CATEGORIES];
                    if (state.tag !== 'All') data = data.filter(c => c.tag === state.tag);
                    if (state.q) {
                        const q = state.q.toLowerCase();
                        data = data.filter(c => c.name.toLowerCase().includes(q) || c.desc.toLowerCase().includes(q));
                    }
                    if (state.sort === 'name') data.sort(by('name'));
                    if (state.sort === 'rating') data.sort(by('rating')).reverse();
                    if (state.sort === 'popular') data.sort(by('rating')).reverse();
                    return data;
                }

                function paginate(data) {
                    const start = (state.page - 1) * state.perPage;
                    const end = start + state.perPage;
                    return { total: data.length, slice: data.slice(start, end) };
                }

                function renderChips() {
                    chips.innerHTML = TAGS.map(tag => `
        <button class="chip ${state.tag === tag ? 'active' : ''}" data-tag="${tag}" aria-pressed="${state.tag === tag}">${tag}</button>
      `).join('');
                }

                function cardTemplate(c) {
                    return `
    <a href="${c.url}" class="card" tabindex="0" aria-label="${c.name} category">
      <span class="badge">${c.rating} rating</span>
      <div class="iconwrap" style="background: radial-gradient(180px 100px at 0% 0%, rgba(255,255,255,.14), rgba(255,255,255,0)), linear-gradient(135deg, ${shade(c.color, -20)}, ${shade(c.color, -35)}); border-color: var(--border)">
        ${circleIcon(c.color)}
      </div>
      <h3>${c.name}</h3>
      <p>${c.desc}</p>
      <div class="meta"><span class="dot" style="background:${c.color}"></span><span>${c.tag}</span></div>
    </a>`;
                }


                function circleIcon(fill) {
                    return `<svg class="i20" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" aria-hidden="true"><circle cx="12" cy="12" r="8" fill="${fill}"/></svg>`;
                }

                function shade(hex, percent) {
                    // simple hex shade util
                    const f = parseInt(hex.slice(1), 16), t = percent < 0 ? 0 : 255, p = Math.abs(percent) / 100;
                    const R = f >> 16, G = f >> 8 & 0x00FF, B = f & 0x0000FF;
                    const r = Math.round((t - R) * p) + R, g = Math.round((t - G) * p) + G, b = Math.round((t - B) * p) + B;
                    return `#${(0x1000000 + (r << 16) + (g << 8) + b).toString(16).slice(1)}`;
                }

                function render() {
                    // chips
                    renderChips();

                    const data = filterData();
                    const { total, slice } = paginate(data);

                    // grid
                    grid.innerHTML = slice.map(cardTemplate).join('');

                    // pagination
                    const pages = Math.max(1, Math.ceil(total / state.perPage));
                    if (state.page > pages) state.page = pages; // clamp
                    pagination.innerHTML = `
        <button class="page-btn" data-page="prev" ${state.page === 1 ? 'disabled' : ''}>\u2039</button>
        ${Array.from({ length: pages }, (_, i) => i + 1).map(p => `
          <button class="page-btn ${state.page === p ? 'active' : ''}" data-page="${p}">${p}</button>
        `).join('')}
        <button class="page-btn" data-page="next" ${state.page === pages ? 'disabled' : ''}>\u203A</button>
      `;
                }

                // ======= Events =======
                searchInput.addEventListener('input', (e) => {
                    state.q = e.target.value.trim();
                    state.page = 1;
                    render();
                });

                sortSelect.addEventListener('change', (e) => {
                    state.sort = e.target.value;
                    state.page = 1;
                    render();
                });

                chips.addEventListener('click', (e) => {
                    const b = e.target.closest('.chip');
                    if (!b) return;
                    state.tag = b.dataset.tag;
                    state.page = 1;
                    render();
                });

                pagination.addEventListener('click', (e) => {
                    const b = e.target.closest('.page-btn');
                    if (!b) return;
                    const pages = Math.max(1, Math.ceil(filterData().length / state.perPage));
                    if (b.dataset.page === 'prev') state.page = Math.max(1, state.page - 1);
                    else if (b.dataset.page === 'next') state.page = Math.min(pages, state.page + 1);
                    else state.page = Number(b.dataset.page);
                    render();
                });



                // Initial render
                render();
            </script>
</body>

</html>